import { SvgIcon } from "@mui/material";
import { HelpCircle } from "react-feather";

export const items = [
    {
      title: 'Quizzes',
      path: '/',
      icon: (
        <SvgIcon fontSize="small">
          <HelpCircle />
        </SvgIcon>
      )
    },
  ];